#include "system.h"
#include <fstream>
#include <sstream>
#include <iostream>
#include <string>
#include <cstdlib>
#include <stack>

void System::loadNodes(const char* fileName) {
    std::ifstream mazefile(fileName);
    std::string n_file, tmp;
    std::stack<Node> tvec;


}
